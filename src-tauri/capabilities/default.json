{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认 capability：core 默认权限 + opener 插件。具体 BC 的命令权限由各模块在 capabilities/<bc>.json 中追加。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
